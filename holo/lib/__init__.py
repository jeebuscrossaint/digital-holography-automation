# -*- coding: utf-8 -*-
"""Numerical helpers: LP mode generation and the FFT/overlap math.

``MMF`` and ``calebsUsefulFunctions`` are Caleb Dobias's originals, kept under
their own names because the multiport engine is verified against his archive
frame by frame and the provenance matters. Import them as submodules:

    from holo.lib.calebsUsefulFunctions import generateModes, modeDecomp

``xenics`` is the vendored Xeneth SDK wrapper for the Bobcat camera.
"""
